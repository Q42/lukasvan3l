# Kopieer naar .env en vul in. .env staat in .gitignore.
SUPABASE_URL=https://kmmhrcdxciyobckrqolc.supabase.co
SUPABASE_SERVICE_KEY=

# Claude: standaard via de Claude Code CLI op je abonnement (geen key nodig).
# Alleen invullen als je liever via de API betaalt:
# ANTHROPIC_API_KEY=
# PARRO_LLM=claude|api        # backend forceren (auto: api als er een key is)
# CLAUDE_BIN=/usr/local/bin/claude   # als 'claude' niet op het cron-PATH staat

PARRO_KINDEREN=Floris, Yune
PARRO_CONTEXT=
# PARRO_DB=
